[user]
    email = {{ .email }}
    name = {{ .name }}
{{ if lookPath "gpg" -}}
[commit]
    gpgSign = true
[gpg]
    program = gpg
[user]
    signingkey = {{ .gpg_key }}
{{- end }}
[push]
    default = simple
{{ if (contains "delta" .git_pager) -}}
[pager]
    diff = {{ .git_pager }}
    log = {{ .git_pager }}
    reflog = {{ .git_pager }}
    show = {{ .git_pager }}
[delta]
    syntax-theme = Dracula
{{- end }}
